{
	"name": "simple-http-client",
	"version": "1.0.0",
	"description": "Simple HTTP client for sending basic HTTP requests and viewing the responses",
	"main": "index.js",
	"scripts": {
		"start": "electron .",
		"test": "echo \"Error: no test specified\" && exit 1",
		"sass": "sass --watch ./styles/src:./styles/dist",
		"ts": "tsc --watch"
	},
	"author": "ekozf",
	"license": "ISC",
	"devDependencies": {
		"electron": "^27.1.0"
	}
}
